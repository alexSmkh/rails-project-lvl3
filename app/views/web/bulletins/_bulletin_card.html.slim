.col-sm-6.col-lg-4.col-xl-3.mb-4
  .card.border.rounded.h-100
    = image_tag bulletin.image,
                width: '100%',
                class: 'card-img-top rounded-top',
                style: 'height: 200px; object-fit: cover;'
    .card-body
      .card-title
        = link_to bulletin.title, bulletin_path(bulletin), class: 'fw-bolder text-decoration-none'
      p.card-text = truncate(bulletin.description, length: 100)
    .card-footer.text-center.align-middle
      p.x-fs-7.mb-0.text-secondary
        = posted_by(bulletin)
